en:
  activerecord:
    errors:
      models:
        reservation:
          already_reserved: 'book is already reserved'
          attributes:
            returned_on:
              past_date: 'cannot be set in the past'
